"use client";

// import { StatisticsView } from '@/components/dashboard/statistics';

export const SettingsSection = () => {

    return (
        <>
            vue des Paramètres
            {/* {mode === "view" && <ProjectsTableView />}
            {mode === "create" && <ProjectCreateView />}
            {mode === "edit" && <ProjectEditView />} */}
        </>
    );
};
